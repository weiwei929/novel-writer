# 已拆分为两种部署模式，请通过 .env 的 CADDYFILE 选择挂载文件：
#
#   CADDYFILE=Caddyfile.http   — 私人 HTTP 手测（默认，http:// 前缀，无 TLS）
#   CADDYFILE=Caddyfile.https  — 公网 HTTPS（全局 email + 自动证书）
#
# docker-compose 将 ${CADDYFILE} 挂载为本文件路径 /etc/caddy/Caddyfile
# 详见 docs/guides/PRIVATE_DEPLOYMENT.md
