主界面UI

Novel-Writer 主界面设计方案基于需求文档（版本1.2）和技术文档（版本1.1），我为你设计了一个简洁、响应式的Web主界面。界面采用Material-UI风格，确保友好交互：暗黑模式支持、拖拽元素、直观导航。整体布局为单页应用（SPA），以Wizard式流程引导创作，但允许自由切换模块。设计原则用户导向：左侧侧边栏导航核心模块（故事设置、生成、编辑等）；中央区域为动态画布（可视化+编辑）；右侧为预览/媒体面板。
响应式：桌面优先，移动端折叠侧边栏。
主题：默认浅色，可切换暗黑；颜色方案：主蓝（#1976d2）用于按钮，灰白背景。
关键元素：Markdown实时预览、拖拽时间线/关系图、媒体上传区。
隐私提示：顶部横幅显示“无审查模式启用，仅限个人使用”。

布局结构（文本可视化）主界面分为三栏布局（Flexbox实现），宽度：左侧20%、中央60%、右侧20%。以下是ASCII艺术粗略草图（想象为浏览器窗口）：

┌─────────────────────────────┬──────────────────────────────────────┬─────────────────────┐
│  [Novel-Writer]             │                                      │  [预览/媒体面板]    │
│  [无审查模式] [设置⚙️] [暗黑🌙] │             中央画布                 │                     │
│                             │                                      │  +----------------+ │
│  � 文集管理                │  ┌──────────────────────────────────┐ │  | Markdown预览 | │
│     - 文集列表              │  │          当前内容区域             │ │  |              | │
│     - 新建文集              │  │                                  │ │  | [渲染视图]    | │
│     - 项目归类              │  │ [文集视图] 或 [项目编辑器]        │ │  +--------------+ │
│  � 故事设置                │  │                                  │ │                     │
│     - 梗概输入              │  │  # 章节标题                      │ │  +----------------+ │
│     - 叙事方式              │  │  **正文...**                     │ │  | 媒体库/文集信息 | │
│     - 人物卡片              │  │  [拖拽插入媒体] [AI续写按钮]      │ │  | - img1.jpg   | │
│  🔗 人物关系图              │  │  [保存] [导出ZIP] [系列管理]      │ │  | - video1.mp4 | │
│  ⏱️ 故事时间线              │  │                                  │ │  | [批量操作]    | │
│  ✨ AI生成                  │  │  [导入范本按钮] [共享设定]        │ │  | [文集统计]    | │
│  ✏️ 编辑与迭代              │  │                                  │ │  +----------------+ │
│  �️ 媒体补充               │  └──────────────────────────────────┘ │  | [上传拖拽区]  | │
│  💾 保存/导出               │                                      │                     │
│                             │                                      │  +----------------+ │
└─────────────────────────────┴──────────────────────────────────────┴─────────────────────┘

顶部工具栏：Logo（Novel-Writer）、免责警告、设置图标（主题切换、API密钥检查）、用户项目列表下拉。
左侧侧边栏：垂直导航菜单，图标+标签；点击切换中央内容。折叠按钮（汉堡菜单）。
中央画布：模块化，根据导航切换：

文集管理：
- 文集网格视图：卡片显示封面、标题、项目数、字数统计、最后更新时间
- 文集详情：项目列表，支持拖拽排序（系列顺序），批量选择操作
- 新建文集：表单（名称、描述、分类、标签选择、封面上传）
- 系列设置：项目间关联图，共享元素配置（人物、世界观、叙事风格）

故事设置：表单堆叠（梗概文本框、叙事下拉、人物卡片列表，支持+添加）。
- 新增：共享设定按钮，可从文集继承人物和世界观
- 新增：项目归属选择器，显示所属文集

人物关系图：React Flow画布，节点拖拽（人物圆圈，边线标注"浪漫/敌对"）。
- 新增：共享人物高亮显示，交叉引用链接

故事时间线：Gantt式条形图，节点可编辑（点击弹出进程细节表单：场景Markdown、冲突多选）。
AI生成：进度条+生成按钮，输入"章节范围"滑块。
编辑：Monaco Markdown编辑器，全屏可选。
- 新增：交叉引用自动补全，输入@角色名自动链接到其他项目

媒体：拖拽上传区，预览缩略图列表。

右侧面板：可折叠，Tab切换。

预览Tab：实时Markdown渲染（HTML视图，支持img/video嵌入）。
媒体库Tab：媒体文件网格，点击插入编辑器（生成![alt](/media/...)）。
文集信息Tab（新增）：
- 当前文集概况：项目数、总字数、完成度进度条
- 系列关系图：显示项目间的前后关系，点击快速跳转
- 共享元素面板：文集共享的人物、世界观列表，可快速插入
- 批量操作：选择多个项目进行统一设置、导出、备份

底部状态栏：字数统计、版本回滚按钮、自动保存提示。

交互流程示例

首次使用：
1. 欢迎向导弹窗：引导"创建第一个文集"→设置文集信息→"新建项目"→归类到文集→输入梗概

文集管理流程：
1. 点击"📚 文集管理"→文集网格视图→点击文集卡片进入详情→显示该文集的所有项目
2. 新建系列：创建文集→标记为系列→添加项目→设置项目顺序→配置共享元素
3. 批量操作：选择多个项目→统一修改叙事风格→自动同步到所有选中项目

跨项目协作流程：
1. 项目A设置人物"李小明"并标记为共享→项目B编辑时输入@李→自动提示并插入链接
2. 修改共享人物设定→系统提示同步到所有使用该人物的项目

创作循环：侧边栏→选择文集→进入项目→时间线设置→中央拖拽节点→右侧预览/文集信息→AI生成按钮→编辑插入媒体。

移动适配：侧边栏变抽屉式，中央全宽，右侧面板变底部Tab（预览/媒体/文集信息）。

技术实现提示（从技术文档）前端：React组件树：<AppLayout><Sidebar /><Canvas /><PreviewPanel /></AppLayout>。
样式：CSS-in-JS (MUI)，响应断点：@media (max-width: 768px) { sidebar: hidden; }。
原型代码片段（伪JSX）：jsx

<Box sx={{ display: 'flex', height: '100vh' }}>
  <Drawer variant="permanent" sx={{ width: 240 }}>
    <List>
      <ListItem button onClick={() => setActive('setup')}>
        <ListItemIcon><BookIcon /></ListItemIcon>
        故事设置
      </ListItem>
      {/* 其他菜单 */}
    </List>
  </Drawer>
  <Box sx={{ flexGrow: 1, p: 3 }}>
  {active === 'collections' && <CollectionManager />}
  {active === 'setup' && <StorySetupForm />}
  {active === 'timeline' && <TimelineChart />}
  </Box>
  <Box sx={{ width: 300, borderLeft: 1 }}>
    <Tabs>
      <Tab label="预览" />
      <Tab label="媒体库" />
      <Tab label="文集信息" />
    </Tabs>
    <TabPanel value="preview">
      <MarkdownPreview source={currentMd} />
    </TabPanel>
    <TabPanel value="media">
      <MediaLibrary onInsert={handleMediaInsert} />
    </TabPanel>
    <TabPanel value="collection">
      <CollectionInfo currentProject={projectId} />
    </TabPanel>
  </Box>
</Box>

这个设计确保高效创作：80%屏幕用于内容，减少点击。如果你想“看到”更视觉化的样子（如PNG mockup或Figma链接），我可以生成一个简单的图像原型——你确认要生成图像吗？（例如，使用工具创建HTML截图或SVG渲染）。如果需要调整细节（如添加具体颜色或功能按钮），告诉我！

