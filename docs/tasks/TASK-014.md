# TASK-014（主页卡）：管道仪表盘 + 继续创作入口

> 前情：TASK-011~013 + 图标已完成，五段导航和 `/work/:id` 中枢已就位。目前 `HomePage` 仍是 1.0 欢迎页。
> 目标：把首页改造为 2.0 管道仪表盘，显示各阶段作品动态 + 强「继续创作」入口。

详见用户提供的完整任务卡（五阶段卡片、继续创作、最近作品、骨架屏）。

## 备注（TASK-016 联动）

「暂存阁」→「作品暂存」涉及：`PROJECT_STATUS_LABEL`、`Layout` 右上角、`ShelfPage` 标题 — 本卡不改。
