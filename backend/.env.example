# Server Configuration
PORT=5000
HOST=0.0.0.0

# Database
# Note: Path is relative to prisma/ folder if using sqlite file
DATABASE_URL="file:../../data/novel.db"

# Security
# Simple password for single-user mode (defaults to 'novel2024' if not set)
APP_PASSWORD=novel2024

# Logging
LOG_LEVEL=info
